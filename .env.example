# Copy to .env.local and adjust. .env.local is git-ignored.
# Every variable is validated at startup in src/env.ts.

# Base URL of the food-be REST API (local: `dotnet run` in ../food-be, launch profile "http")
VITE_API_URL=http://localhost:5201
