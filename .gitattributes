# Normalize all text files to LF in the repo and in the working tree (overrides core.autocrlf)
* text=auto eol=lf

# Binary files – never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
